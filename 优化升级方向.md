# 优化升级方向

对比同类商业产品（Eightify、Glarity、Notta、Otter.ai、Language Reactor、Descript）的差距分析与升级建议。

---

## 第一档：高价值、低难度（可立即做）

| 功能 | 现状 | 商业产品做法 | 实现代价 |
|------|------|-------------|:--:|
| **播放列表批量处理** | 单个URL | 粘贴播放列表 → 排队处理全部视频 | `batch` 端点 + 前端队列 |
| **导出格式扩展** | 仅 .md | .md / .srt / .pdf / Notion / Obsidian | 加 format 参数 |
| **多视频对比** | 无 | 选中 2+ 视频 → LLM 并排对比异同点 | 一个 API + 前端 diff 视图 |
| **标签/分类系统** | 无 | 自动分类(AI教程/科技/VPS) + 手动标签 | `meta.json` 加 tags 字段 |
| **SRT 字幕导出** | 无 | 下载 .srt 导入播放器 | `SubtitleEntry → SRT` 格式化函数 |
| **域名模板选择** | 无 | 预设分析模板(教程/讲座/会议/娱乐) | prompt 参数化 |

---

## 第二档：中价值、中难度

| 功能 | 现状 | 商业产品做法 | 实现代价 |
|------|------|-------------|:--:|
| **说话人分离** | 无 | 访谈/播客区分 A/B/C 说话人 | faster-whisper + pyannote.audio |
| **时间戳跳转** | 静态展示 | 点击重点 → 跳转到视频对应时间 | 嵌入 YouTube iframe + `?t=` 参数 |
| **跨视频语义搜索** | 单视频 Chat | "我所有视频里谁讲过XX" | 全局 ChromaDB + 单选视频 vs 全库 |
| **关键帧截图** | 无 | 每个重点附带视频画面截图 | yt-dlp 下载帧 + LLM 选关键时刻 |
| **浏览器插件** | 无 | 网页上一键发送到工具 | Chrome Extension manifest |
| **思维导图生成** | 无 | 从视频内容生成知识图谱 | LLM 输出 Mermaid 格式 |
| **频道订阅监控** | 无 | 订阅 YouTuber，新视频自动分析通知 | Cron + RSS/API 轮询 |
| **共享/协作** | 本地单机 | 分享链接、导出 Notion 页面 | 生成静态 HTML 页面分享 |

---

## 第三档：高价值、高难度

| 功能 | 现状 | 商业产品做法 | 实现代价 |
|------|------|-------------|:--:|
| **多模态分析** | 纯文本 | 分析画面：PPT截图OCR、人物识别、场景检测 | 需要视觉模型(GPT-4V/Gemini) |
| **行动项提取** | 无 | 教程视频 → "需要做的事"清单 | 专用 prompt + 结构化输出 |
| **自定义分析模板** | 固定 prompt | 用户可编辑分析维度 | 前端编辑器 + prompt 变量替换 |
| **移动端适配** | 桌面 Fixed 布局 | 响应式 UI，手机可用 | CSS media query 重构 |
| **音频文件上传** | 仅 URL | 上传 mp3/mp4 → Whisper 转录 | 文件上传组件 + 直接调 transcribe |
| **API 对外开放** | FastAPI 未鉴权 | 提供 API Key → 程序化调用 | 简单的 token 认证中间件 |
| **增量索引** | 每次重建 | 新增视频只索引新内容 | 持久化元数据 + 去重逻辑 |

---

## 第四档：长远愿景

| 功能 | 描述 |
|------|------|
| **学习路径规划** | 从系列视频提取知识点 → 生成学习路线图 + 先修关系 |
| **知识图谱** | 跨视频连接人物/概念/事件 → 可交互的可视化图谱 |
| **语音克隆+TTS配音** | 翻译后生成同声 TTS 配音（多人协作场景） |
| **视频内搜索定位** | 输入关键词 → 定位到视频中该词出现的精确秒数 |
| **平台扩展** | 抖音、TikTok、Vimeo、网易公开课、Coursera |
| **企业版协作** | 团队空间、权限管理、分析模板共享 |

---

## 建议实施顺序

```
1. SRT 字幕导出        ← 1小时，补最明显的功能缺口
2. 时间戳跳转          ← 2小时，用户体验质变
3. 播放列表批量处理     ← 3小时，解放重复操作
4. 跨视频全局搜索       ← 4小时，从"单视频工具"变"知识库"
5. 标签/分类系统        ← 2小时，内容管理基础
6. 关键帧截图          ← 4小时，重点标示不再只是文字
7. 导出格式扩展        ← 3小时，覆盖不同使用场景
8. 浏览器插件          ← 6小时，降低使用门槛
9. 频道订阅监控        ← 4小时，自动化内容获取
10. 思维导图生成       ← 3小时，知识结构化
11. 说话人分离         ← 8小时，访谈/播客场景
12. 多模态分析         ← 16小时，画面理解
```

---

## 架构预备

以下功能在实施前需要先做架构调整：

### 播放列表/批量处理 → 任务队列

当前 `POST /api/analyze` 直接启动后台线程。批量处理需要：
```
POST /api/batch → 创建任务队列
  → 逐个 POST /api/analyze
  → SSE 推送总体进度 (3/15 完成)
  → 全部完成后汇总
```

### 跨视频搜索 → 全局索引

当前 ChromaDB 每个视频一个 collection。全局搜索需要：
```
新增 global 索引：所有视频段落 → 统一 collection
  → search(global, q, k=10)
  → 结果带 source_video 字段标识来源
  → 历史 collection 保持不变（单视频问答仍可用）
```

### 标签/分类 → 扩展 meta.json

```
meta.json 新增字段:
  "tags": ["AI", "教程", "Claude Code"],
  "category": "科技",
  "language": "zh"
```

自动分类：将 overview 摘要送 LLM 判断分类。
